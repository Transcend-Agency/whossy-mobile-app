import 'package:flutter/material.dart';

class ProfileViewScaffold extends StatelessWidget {
  const ProfileViewScaffold({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
